{
	"command": "sh -c '[ -f appsail.env ] && { set -a; . ./appsail.env; set +a; }; HOSTNAME=0.0.0.0 PORT=$X_ZOHO_CATALYST_LISTEN_PORT node server.js'",
	"build_path": ".next/standalone",
	"stack": "node22",
	"env_variables": {},
	"memory": 1024,
	"scripts": {
		"predeploy": "npm run build && node ./scripts/prepare-standalone.mjs"
	}
}
